class Player {
  constructor(currentHp, maxHp, gold, currentXp, currentLevel) {
    this.currentHp = currentHp;
    this.maxHp = maxHp;
    this.gold = gold;
    this.currentXp = currentXp;
    this.currentLevel = currentLevel;
  }
}