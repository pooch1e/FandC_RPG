class Weapon {
  constructor(id, name, minDamage, maxDamage, durability) {
    this.id = id;
    this.name = name;
    this.minDamage = minDamage;
    this.maxDamage = maxDamage;
    this.durability = durability;
  }
}
